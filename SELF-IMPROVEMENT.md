# dreaming.press — self-improvement loop (20 loops)

Each loop: (1) score via `cd app && node scripts/eval-harness.js` → `app/eval-log.jsonl`,
(2) **browser visual QA**: `npm run qa:visual` (headless-Chrome layout assertions on 7 pages
× 2 viewports: nav wrap, footer grid, overflow, template artifacts, console errors) — find
issues by LOOKING, not just presence-checks, (3) consult **DESIGN-REVIEW.md** (the LLM design
council's 13 ranked moves + homepage spec; re-run a council when the plan is exhausted or the
direction shifts) and pick enhancements, (4) implement, (5) `npm test` + HTTP smoke (≥20 pages)
+ `qa:visual` all green, (6) deploy only if green and improved. Screenshots land in
/tmp/dp-vqa-*.png every run for eyeball review.

## Score history
| Loop | Overall | Notable dims | What shipped |
|------|---------|--------------|--------------|
| baseline | 8.50 | audio 1.2, analytics 7.5 | — |
| 1 | **8.87** | analytics 7.5→10 | Eval harness + first-party `/dashboard` (SVG trend, funnel, channels, referrers, top content) |
| 2 | **9.31** | audio 1.2→5.6 | In-browser "Listen" (Web Speech API) for all 682 no-audio posts; harness credits listenability |
| 3 | **9.53** | quality 8.9→9.9 | Fixed harness tag-scoring bug (array vs JSON string) + ingest backfills deks from opening sentence (0 missing) |
| 4 | **9.53** | UX (real: 9.38→9.53) | Paginate section pages — The Wire was **581 posts on one page**; now 30/page w/ prev-next + per-page canonical. ~95% page-weight cut. Harness gained a real page-weight signal. |
| 5 | **9.53** | UX (real: 9.38→9.53) | Paginate **tag** (reportive=680) + **author** (dex=508) archives via shared pageWindow/pagerNav. Harness signal generalized to all listing types. |
| 6 | **9.53** | structure (internal-link health) | Added a real **internal-link canonicalization** signal: `redirectInternalLinks` in check-content.js flags de-dated cross-links that only resolve via a 301 alias hop (the existing `deadInternalLinks` strips dates → caught only 404s). Fixed all 8 corpus offenders; 1892 internal links now resolve direct (0 broken, 0 redirect). Unit-tested. |
| 7 | **9.53** | UX (404 recovery) | 404 was a dead end; now a recovery surface — search form + 6 recent pieces (NYT/Guardian pattern). Verified live. Harness `notFoundRecovery` signal. |
| 8 | **9.53** | UX (CWV: render-blocking) | Google Fonts CSS was a **render-blocking third-party stylesheet** in the critical path (DNS+TLS+request to fonts.googleapis.com before first paint) — a direct FCP/LCP cost, and LCP is a ranking signal. Moved it off-path via `media="print"`+`onload` swap (+`<noscript>` fallback; `display=swap` already handled FOIT). New harness `noRenderBlockingFontCss` signal renders a real page head and passes only when every foreign-origin sheet loads non-blocking; verified it fails on the old markup and passes on the fix. |
| 9 | **9.53** | structure (PWA) | Installable **web app manifest** + 192/512/maskable icons + `mobile-web-app-capable` (was missing → Lighthouse PWA flag). Verified live. |
| 10 | **9.53** | UX/a11y + image-SEO | Hero cover `<img alt>` was `alt="${title}"` on every article — a redundant echo of the `<h1>` directly below it (screen readers announce the headline twice) and worthless to image search. The `art:` frontmatter already carries a `motif` (a concrete description of the generative cover), so the hero alt now uses the **motif** (capitalized/whitespace-normalized), falling back to the title only when a piece has none. Added a `render.test.js` assertion (motif→alt; no-motif→title fallback). Verified live: the new OTel piece's cover alt renders "A trace waterfall with five of its nine rows erased to blank space" — a real image description, not the headline. |
| 14 | **9.53** | visual QA (browser-found) | **Visual-QA harness** (`npm run qa:visual`, 27 layout assertions) + fixed 4 browser-found bugs: nav labels wrapped mid-item (ragged baselines), footer 4-col grid with a 23-link column orphaned 'The press' onto a lonely row (rebuilt as brand-row + auto-fit `.f-cols`, mega-column split), '2 reads reads' dup, /favicon.ico 404 on every page, bare native search input → brand pill. **LLM design council** (5 lenses, 30 findings) → `DESIGN-REVIEW.md`: 13 ranked moves + homepage Briefing spec — the roadmap the cloud routine now executes every run, gated on qa:visual. All verified live. |
| 15 | **9.53** | HOMEPAGE → Global Tech News | **News-first front-page rebuild** (council Moves 1+2 core): The Briefing top-5 digest (summary bullets, ▶ play-the-briefing), edition dateline, Wire band w/ TODAY/YESTERDAY headers, How-tos+calculators strip, Dispatches/Fabrications demoted to 'From the machines', global dedupe (29 placements 0 dups), single CTA. Live-verified; harness 29/29 incl new dedupe assertion. |
| 16 | **9.53** | brand identity + IA | **Nameplate masthead** (cloud routine: centered Fraunces wordmark on double rule, scroll-reveal sticky brand, one blinking element) + **task-labeled nav** (session: News · How-tos · Tools & Reviews · Concepts · Calculators · For Founders; desk names left the nav). The 'still looks like the original' complaint is structurally addressed. Live-verified; 2327 unit, 36/36 smoke, 29/29 visual-qa. |
| 17 | **9.53** | transparency (Move 7) | **Public metric chips** on every card/wire-row via `db.attachMetrics()` (one grouped query, 60s cache): '5 min · N reads' at every click decision across home/sections/tags/authors. 23 chips live on the homepage. |
| 18 | **9.53** | time-on-site (Move 6) | **'Up next' next-read system** complete: hero unit after the body (cloud routine) + **sticky reveal bar at 85% scroll** + **sources folded into <details>** when ≥4 refs (session) — the next click is always on screen when a reader finishes. Live-verified. |
| 19 | **9.53** | owned audience (Move 8) | **Newsletter capture system**: filled Subscribe masthead pill → `/subscribe` page (founder-brief promise, honest social proof, sample stories); in-article capture after Up-next; ctaBand reframed. All forms source-tagged for channel attribution. Live: /subscribe 200. |

## Backlog (weakest-first)
- **audio 1.2/10** — only ~12% of 775 posts have narration. Biggest single gap. Needs a
  batch TTS pass (Kokoro in `tts/`) — heavy; do incrementally (recent/high-engagement posts first).
- **quality 8.9** — lift median depth / source coverage on thin pieces.
- Harden harness with real Lighthouse/CWV + accessibility signals (don't inflate on presence alone).
- Research best publications (NYT/Guardian/Verge/Stratechery/Pudding) → import one concrete pattern per loop.
- UX/structure are 10 on presence checks — make the harness stricter so they have headroom (real a11y, CWV, INP).

## Method note
The presence-based dims (ux/art/structure/analytics) sit near 10, so each loop now
(a) adds a **real, continuous signal** the current site may fail, then (b) fixes it —
this keeps improvements honest and measurable (loop 4 did this for page-weight).

## State
- **Loop 10 of 20 complete — DIRECTION CHANGED** (optimize ONLY for visitors + time-on-site; audience = founders/CEOs/solopreneurs). Shipped public metrics + reoriented the newsroom. **Next big builds (Loops 11+):** (a) HOMEPAGE → Global Tech News digest (aggregate top news from every source, summarized + per-item audio) — the flagship restructure; (b) rework the eval harness dimensions around visitors/time-on-site/measurement-transparency/news-freshness/how-to-value (retire art/audio weight toward engagement); (c) analytics agent that auto-commissions from top pages; (d) how-to/tutorial/tool/API content surfaces + nav (COMPARISONS/CONCEPTS/CALCULATORS exist; add GUIDES/TOOLS/NEWS). 

- **Loop 11 (2026-07-10, cloud-routine) — first execution of the founder-facing direction.** Prior runs kept shipping deep AI-infra Wire/Stack pieces; this run wrote the first two pieces *for the new audience*: a **global tech-news roundup for founders** (`ai-news-for-founders-july-2026`, Wire — model price war vs. capital concentration; GPT-5.6/Grok 4.5/GLM-5.2 pricing, the 89% revenue-share stat, $19B TeraWulf lease, General Intuition/8090/Kling rounds; every fact verified via parallel research + WebSearch, real sources cited) and a **founder LLM-API buyer's guide** (`how-to-choose-an-llm-api-without-lock-in`, Stack — OpenAI-compatible routing, eval set, output-token math, open-weights thresholds; code samples). **Part B seeds roadmap item (d):** new LAST comparison cluster **"AI for Founders"** in `lib/db.js` — a distinct founder-news/strategy buyer-intent class beside the engineering hubs; both pieces home into a mutual sibling rail + indexable `/comparisons/ai-for-founders` hub, with reserved tokens (for-ceos/solopreneur/moat/news-roundup/state-of-ai/…) awaiting the recurring roundup format. Corpus-scanned: rescues exactly the 2 catch-all pieces, poaches 0. ingest 783; 2 covers 0 errors; suite **2311 pass / 0 fail**; SSR smoke — both articles + the new cluster page 200, cluster rail + cross-links render. **Env note:** `/api/analytics` still egress-blocked (dreaming.press:443 → 403 CONNECT, policy denial — consistent with every prior run), so commissioning was corpus- + direction-driven, not live-numbers-driven; the analytics-first step remains structurally impossible under this session's egress policy until the host is allowlisted.
- **Loop 12 (2026-07-10, cloud-routine) — second founder-direction run: fresh roundup + tools + recirculation.** Part A shipped 2 pieces, both homing into "AI for Founders" (slugs carry cluster tokens): a **mid-July global news roundup** (`the-demand-side-ai-price-war-for-founders`, Wire — the week's distinct angle vs. Loop 11: the AI premium being competed away from the *demand* side — Microsoft routing Excel/Outlook to in-house MAI models + Suleyman "eliminate that cost" quote, US enterprises at 46% of OpenRouter tokens on Chinese open models, Nvidia's ~$1T slide, China's capped H200 thaw, Blue Origin $10B/$130B, Meta Muse; every fact verified via 2 parallel research sub-agents + WebSearch spot-checks on the two load-bearing Bloomberg stats) and an **AI cost-control tools roundup** (`ai-cost-control-tools-for-founders`, Stack — OmniRoute/Stigg/Meetily/Herdr/Strix as the plumbing to *capture* the price war's savings; @repo blocks, compare/figures/faq). Rich frontmatter (summary/compare/figures/faq) on both for time-on-site; mutual cross-links + the how-to-choose guide. **Part B (roadmap item toward b/engagement):** new **"Latest from The Wire" pure-recency rail** on Wire article pages only (`render.js` + `server.js`) — news readers finishing a dated roundup have thin topic-siblings, so `relatedTo`'s "Continue reading" can only offer loosely-related/older posts; the rail surfaces the freshest headlines (the "Latest" pattern every news site runs), deduped against self + Continue-reading, reusing `postsBySection`. Additive optional `latestNews` param (default `[]` → every existing caller/test byte-identical). ingest 785; 2 covers 0 errors; suite **2316 pass / 0 fail** (+1 new render test); SSR smoke — both new articles 200 with compare/faq/@repo/cross-links; rail present on Wire, absent on Stack. **Deploy note:** direct `main` push is intermittently rejected as non-fast-forward *despite a clean fast-forward* (a build-window lock; `ls-remote` confirms the true tip is the base I'm on) — matches the `diag-push-test`/`newsroom-*` branch graveyard prior runs left. Workaround: push to a dated `newsroom-<date>-<hash>` branch as a safety net, then retry `HEAD:main` on a short backoff — it clears within a minute or two. Both commits (`f1af2b0` content, `86cb037` rail) landed on `main`. **Env note:** `/api/analytics` still egress-blocked (403 CONNECT), so commissioning stays corpus-/direction-driven.
- **Next up (Loops 13+):** keep the founder-news cadence (roundup at each news event, distinct weekly angle); bigger builds still open — (a) HOMEPAGE global-news digest, (b) eval-harness reweight toward engagement/freshness (the recency rail is a first user-facing step; the *measurement* half is still owed), (c) analytics auto-commissioning (blocked until `/api/analytics` reachable), (d) GUIDES/TOOLS/NEWS nav surfacing the new cluster.

- **Loop 13 (2026-07-10, cloud-routine) — founder how-to + nav surfacing (roadmap item d, first slice).** Two founder roundups already ran today (Loops 11–12), so Part A shifted format to the under-served **how-to** slot on the week's hot topic (cost/the price war): `how-to-cut-your-llm-bill-for-founders` (dex, Stack) — spine: *the biggest LLM savings never touch the model your users see; the same request costs wildly different amounts by how you send it, not which model.* Five ROI-ordered moves (prompt caching → gateway → model tiering behind evals → semantic/answer caching → batch + hard spend cap), four invisible to users, model-downgrade explicitly last. topic-check clear (40% max, "cut your llm bill" was un-owned; the corpus's ~50 cost pieces are all engineer/agent-facing, not founder-app-facing). Facts fact-checked by a research sub-agent: **Anthropic numbers first-party verified** (cache read 0.1x, 1.25x write, batch 50%, Opus $5/$25 etc.); OpenAI/Gemini/LiteLLM pages 403'd in-env so those figures are phrased as "reported/around" and triangulated across WebSearch snippets + the real doc URLs. Slug carries the `for-founders` token → auto-homes into the **AI for Founders** cluster (verified `clusterLabelFor`), no db.js edit needed; 4 in-cluster internal links; full demand kit (summary/compare/figures/faq/sources/art grid·hopeful). **Part B (roadmap item d):** added a **"For Founders"** entry to the primary masthead nav (`render.js`) pointing at the indexable `/comparisons/ai-for-founders` hub — the growth audience (founders/CEOs/solopreneurs) was one click deep, buried among ~40 engineering comparison clusters; now it's top-nav. The founder hub owns the active-state highlight (Comparisons de-highlights there); +1 render test. **Verification:** ingest **786**; gen-art 1 webp/avif 0 errors; suite **2319 pass / 0 fail** (+1); live SSR :3003 — article 200 (correct `<title>`, AI-for-Founders rail, 4 links resolve), hub 200 (new piece listed, "For Founders" nav aria-current, Comparisons not), homepage 200 (nav link present). Both commits landed on `main` via direct `HEAD:main` push (f43de83→13ab6c9 content, →f38a345 nav). **Env note:** `/api/analytics` still egress-blocked (proxy `recentRelayFailures` shows `dreaming.press:443 → 403 CONNECT`, policy denial) — the analytics-first commissioning step remains structurally impossible until the host is allowlisted, so commissioning stayed corpus-/direction-driven.
- **Loop 14 (2026-07-10, cloud-routine) — two Part-B fixes riding a fresh-angle Part-A run.** Part A shipped 2 founder pieces on a *distinct* angle (agents-taking-real-actions/liability roundup + a programmatic-SEO-with-Next.js how-to), avoiding today's over-covered price-war theme. Part B (product): **(1)** fixed a structural blocker on the mission's #2 content type — `renderMdTwin` (`lib/pages.js`) stripped code tags then globally unescaped entities, resurrecting escaped JSX (`<h1>`/`<p>`) from code blocks into un-fenced raw HTML, which failed the twin's raw-HTML guard for *any* code-heavy how-to; now code is held out as placeholders and restored as fenced ``` blocks (verbatim, correctly formatted), and the test's guard is scoped to prose. This unblocks all future tutorials with HTML/JSX/shell samples. **(2)** shipped the Loop-13 next-up lead: audience-aware `/comparisons/:slug` hub copy (`renderComparisonCluster`) — the founder hub now reads "for **founders building AI companies**" instead of the generic "for building AI agents" (other clusters unchanged). Full suite **2323 pass / 0 fail**; SSR-verified. `/api/analytics` still egress-blocked (403 CONNECT).
- **Next up (Loops 15+):** the bigger builds are still open — (a) HOMEPAGE global-news digest; (b) eval-harness reweight toward engagement/freshness (measurement half still owed); (c) analytics auto-commissioning (blocked until `/api/analytics` reachable); (d) GUIDES/TOOLS/NEWS nav surfacing beyond the single "For Founders" entry. Fresh lead: the `.md` twin now carries clean fenced code — worth a per-post "copy as markdown / view raw" affordance to lean into the code-along dwell-time value.

- Deploy contract unchanged: push to `main` → gil-vm pulls every ~10 min. Always `git pull --rebase` first (hourly newsroom shares the repo).
